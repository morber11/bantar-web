services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-}
        VITE_APP_ENV: ${VITE_APP_ENV:-}
    image: bantar-frontend:latest
    ports:
      - "4173:80"  
    restart: unless-stopped
