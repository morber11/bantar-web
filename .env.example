# Example environment variables for bantar-web
# API base URL - if empty, the app will use the Vite dev server proxy for /api
VITE_API_URL=
# Set this to 'prod' to use the production proxy in vite.config.ts if applicable
VITE_APP_ENV=local
